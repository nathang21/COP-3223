scanf(fileName);
FILE* IFP;
ifp = fopen(word, "r") // r means read
fscanf(IFP, "%d", &n);

// program whatever sum

// output

printf(output);
scanf(filename)
OFP=fopen(output, "w") // w means write
fprintf(OPF, "%f", sum);
fclose(IFP);
fclose(OFP);

return 0;
